func get_fib() {
    func fib(n) {
        return n > 2 ? fib(n - 1) + fib(n - 2) : n;
    }
    return fib;
}
var fib = get_fib();
var start = time();
for (var i = 0; i < 10; i = i + 1) {
    print fib(i);
}
print "It took this many seconds to execute: ";
print time() - start;
